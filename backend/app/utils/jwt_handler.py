"""
Legacy JWT helpers have been moved to `utils/deprecated/jwt_handler.py`.
This file is intentionally inert. Use Supabase Auth via `utils.auth_dependencies.get_current_user`.
"""
raise SystemExit('jwt_handler removed: use Supabase Auth')
